<h2>Meeting</h2>

<p><a href="#" class="text-primary">Primary link</a></p>

<div class="whiteboard row">
  <div class="happy col">
    <ul class="list-inline">
      {{#each model as |whiteboard_item| }}
        {{whiteboard-item item=whiteboard_item }}
      {{/each}}
    </ul>

    <form>
      {{input value=newWhiteboardItem type="text" placeholder="Describe your item"}}
      <button {{action 'createWhiteboardItem'}}/>
    </form>
  </div>

  <div class="meh col">
    <ul class="list-inline">
      {{#each model as |whiteboard_item| }}
        {{whiteboard-item item=whiteboard_item }}
      {{/each}}
    </ul>

    <form>
      {{input value=newWhiteboardItem type="text" placeholder="Describe your item"}}
      <button {{action 'createWhiteboardItem'}}/>
    </form>
  </div>

  <div class="sad col">
    <ul class="list-inline">
      {{#each model as |whiteboard_item| }}
        {{whiteboard-item item=whiteboard_item }}
      {{/each}}
    </ul>

    <form>
      {{input value=newWhiteboardItem type="text" placeholder="Describe your item"}}
      <button {{action 'createWhiteboardItem'}}/>
    </form>
  </div>
</div>
